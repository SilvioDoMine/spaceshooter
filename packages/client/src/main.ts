import { DEFAULT_GAME_CONFIG, GameConfig, Vector2D } from '@spaceshooter/shared';

console.log('Cliente iniciado');
console.log('Config do jogo:', DEFAULT_GAME_CONFIG);

const position: Vector2D = { x: 100, y: 200 };
console.log('Posição inicial:', position);